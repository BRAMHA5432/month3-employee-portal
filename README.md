# Employee Portal (Month 3 Project)

Simple Employee Portal with basic authentication (Spring Security), roles, and a dashboard.

## Features
- Login (in-memory users for demo)
- Role-based pages (ADMIN / USER)
- Dashboard placeholder
- H2 database for storing employees if extended

## Run
mvn spring-boot:run
Access login page: http://localhost:8081/login
